# ============================================================
# Environment Variables — Copy this file to .env and fill values
# ============================================================

# PostgreSQL Database Connection
DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/univ_slots

# SMTP Email Configuration (for notification emails)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=admin@yourdomain.edu
SMTP_PASSWORD=your_app_password
